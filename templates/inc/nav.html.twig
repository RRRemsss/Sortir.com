{% if app.user %}
<nav class="navbar navbar-expand-lg py-3 text-bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('main_home') }}">
        <img src="{{ asset('img/logo/Logo_sortir.png') }}" width="80" height="80"  role="img" class="d-inline-block align-top logo"  alt="">
    </a>
    <button class="navbar-toggler text-white" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon burger"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link custom-nav" href="{{ path('main_home') }}" title="Homepage">Accueil</a>
        </li>
        <li class="nav-item">
          <a class="nav-link custom-nav" href="{{ path('user_index', {'id': app.user.id}) }}">Profil | {{ app.user.username|title }}</a>
        </li>
        
        
      </ul>
        <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
            <ul class="navbar-nav">
                {% if is_granted("ROLE_ADMIN") %}
                <li class="nav-item">
                    <a class="nav-link custom-nav" href="{{ path('admin_dashboard') }}">Admin</a>
                </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link custom-nav" href="{{ path('app_logout') }}">Deconnexion</a>
                </li>
            </ul>
        </div>
    </div>
  </div>
</nav>
{% endif %}
{% if not app.user %}
<nav class="navbar navbar-expand-lg py-3 text-bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('main_home') }}">
        <img src="{{ asset('img/logo/Logo_sortir.png') }}" width="80" height="80"  role="img" class="d-inline-block align-top logo"  alt="">
    </a>
  </div>
</nav>
{% endif %}