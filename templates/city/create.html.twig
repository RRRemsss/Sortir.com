{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Ajouter une Ville{% endblock %}

{% block body %}
    <h1 class="text-center mt-3">Ajouter une Ville</h1>
    <div class="container">
        {#todo: afficher le formulaire de création #}
        {{ form_start(cityForm) }}
        {{ form_widget(cityForm) }}

        <a href="{{ path('admin_villes') }}" class="btn btn-dark">Retour</a>
        <button class="btn btn-success">Ajouter</button>
        {{ form_end(cityForm) }}
    </div>
{% endblock %}